# REMOVED — superseded by app/pipeline/preprocessor.py (Phase 3 canonical)
# This file is intentionally left as a stub to avoid stale .pyc issues.
# Do not import from this module.
raise ImportError(
    "image_processor.py has been replaced by preprocessor.py (Phase 3 canonical rename). "
    "Import from app.pipeline.preprocessor instead."
)
